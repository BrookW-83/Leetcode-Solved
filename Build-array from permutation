class Solution:
    def buildArray(self, nums: List[int]) -> List[int]:
        number = 0
        answer = []

        for index in range(0, len(nums)):
            number = nums[nums[index]]
            answer.append(number)

        return answer
